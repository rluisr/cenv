project_name: cenv
before:
  hooks:
    - go mod tidy
builds:
  - main: .
    binary: cenv
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    goarm:
      - "7"
release:
  github:
    owner: rluisr
    name: cenv
  prerelease: auto